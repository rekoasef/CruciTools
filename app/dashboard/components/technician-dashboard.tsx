// RUTA: /app/dashboard/components/technician-dashboard.tsx

// Este componente no es necesario ya que el contenido se renderiza directamente en /dashboard/page.tsx
// Se deja como placeholder.

export default function TechnicianDashboard() {
    return (
        <div className="p-8 text-center text-gray-500">
            Cargando el dashboard del técnico... (El contenido se renderiza en la página principal).
        </div>
    );
}